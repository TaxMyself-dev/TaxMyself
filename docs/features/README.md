# Implemented features

These documents explain behavior that exists in the code. Keep them focused on
stable flows, invariants, configuration names, and operational checks. Detailed
working instructions belong in the nearest code-topic `AGENTS.md`.

If a feature document conflicts with current code or tests, update it in the
same task. During the accounting redesign, the binding redesign master plan
overrides descriptions of the pre-redesign accounting model.
