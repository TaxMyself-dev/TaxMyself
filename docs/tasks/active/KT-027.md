# KT-027 - Preserve VAT report period across document review

- Status: `PUSHED`
- Task manager: Primary integration manager
- Worker: Primary manager
- Worktree: `C:\Users\harel\.codex\worktrees\kt027\taxmyself-dev`
- Branch: `codex/kt-027-vat-review-period`
- Base commit: `a6addf44`

## Business outcome

Returning from document/expense review to a VAT report keeps the selected
business, year, and bimonthly period visible and reloads that same report.

## Acceptance criteria

- [x] Both VAT review entry paths carry the selected filter state.
- [x] Returning from review restores the filter controls and report dates,
      including July-August 2025 instead of the current-year default.
- [x] Existing monthly/bimonthly business eligibility remains enforced.
- [x] Focused isolated regression assertion and Angular production build pass.

## Scope and constraints

- In scope: VAT report/review navigation state and shared restoration helper.
- Out of scope: report calculations, accounting data, schema, production data.
- Required reading: root and topic AGENTS, redesign master plan, coordination workflow.
- Approval boundaries: no production deployment or data changes.

## Handoff

- Result: VAT review navigation now round-trips filter controls as well as
  report request dates; the bimonthly selector's separate default-month value
  is restored too.
- Commit hash(es): recorded after local worker commit.
- Changed files: VAT report page/topic documentation, shared filter navigation
  helper and focused spec, this task record.
- Tests and exact results: isolated Node/TypeScript assertion for 7-8/2025
  versus 1-2/2026 passed; Angular production build passed (exit 0).
- Known pre-existing failures: `ng test --include=...` compiled unrelated
  legacy specs and third-party declarations, failing before the target spec
  could execute. This repository-wide runner behavior is documented in
  `docs/coordination/LESSONS.md` section 4.
- Schema / production / security / accounting / external impact: None.
- Risks and manual checks: after frontend deploy, select July-August 2025,
  enter review through the new-documents banner, approve, and verify both
  filter controls and report figures still refer to July-August 2025.
- Documentation updated: `frontend/src/app/pages/vat-report-journal/AGENTS.md`.

## Integration

- Reviewed by: primary integration manager; diff and `git show --check` clean.
- Integrated commit: `1774d02c` (on current `origin/main` base `5973091c`).
- Combined verification: Angular production build passed; worker's isolated filter assertion passed. The legacy Angular test runner remains unsuitable for focused isolation.
- `origin/main` verification: `be15a03c` confirmed after push.
- User-visible run instructions: after a separate frontend deployment, select July-August 2025, review new documents, and verify the report remains on that period.
