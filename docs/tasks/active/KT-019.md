# KT-019 - Show linked expense files in the VAT report

- Status: `PUSHED`
- Task manager: Codex `/root`
- Worker: Codex `/root` (single-agent execution)
- Worktree: `C:\Users\harel\.codex\worktrees\kt019\taxmyself-dev`
- Branch: `codex/kt-019-vat-attachments`
- Base commit: `193274cfec0026491b655df4ac09d4fd68cd0ded`

## Business outcome

The VAT report expense table consistently shows and opens an expense's attached document, whether it was uploaded manually or originated from the Drive/OCR flow.

## Acceptance criteria

- [x] VAT input rows expose the linked source-document identity without changing report totals.
- [x] A manually uploaded expense file keeps the existing preview, download, replace, and delete behavior.
- [x] An expense linked only to a Drive source document is shown as having a file and can be previewed.
- [x] When both attachment paths exist, the manually uploaded file remains the active attachment in the VAT table.
- [x] The attach-file control is shown only when neither attachment path exists.
- [x] Focused backend test and both production builds pass; frontend focused specs were added, but the legacy Angular runner compiles unrelated broken specs before executing selected tests.

## Scope and constraints

- In scope: VAT report response metadata, VAT expense-table file state/actions, focused tests, and topic documentation.
- Out of scope: schema changes, production data changes, accounting calculations, report totals, source-document replacement/deletion, and production deployment.
- Required reading: root `AGENTS.md`, reports/expenses/VAT topic docs, coordination workflow and quality gates, categories redesign audit and master plan.
- Approval boundaries: no schema, accounting, authorization, external API, or production mutations.

## Handoff

- Result: complete and integrated after KT-018 reached `origin/main`.
- Commit hash(es): `691012eb`
- Changed files: VAT report DTO/query, VAT report page/spec, generic table attachment detection/spec, reports/VAT topic docs, this task file.
- Tests and exact results: backend focused Jest `1 passed`; Nest build exit 0; Angular production build exit 0 with existing budget/CommonJS warnings.
- Known pre-existing failures: focused `ng test --include` compiled the repository's unrelated legacy specs and stopped before execution on their known type/import errors (plus missing Node typings); the local generic-spec type error exposed in that output was corrected.
- Schema / production / security / accounting / external impact: no schema or production data change; no authorization, accounting totals, VAT membership, or external API mutation. The VAT response gains additive attachment metadata.
- Risks and manual checks: visually verify a manual attachment, a Drive source attachment, and an expense with neither attachment in the VAT table after integration/deployment.
- Documentation updated: `backend/src/reports/AGENTS.md` and `frontend/src/app/pages/vat-report-journal/AGENTS.md`.

## Integration

- Reviewed by: Codex `/root`
- Integrated commit: `f5fd805d` (implementation) and `a7b6c77e` (worker handoff), based on current main `5a099fb2`.
- Combined verification: focused backend Jest passed (`1 passed`); Nest build exit 0; Angular production build exit 0 with existing style-budget/CommonJS warnings; `git diff --check` passed.
- `origin/main` verification: delivery push verified at `8f1078ce`; local `main`, `origin/main`, and `codex/integration` matched before this status-only commit.
- User-visible run instructions: after deployment of both backend and frontend, open a VAT report containing one Drive/OCR expense and one manually attached expense; both should show file actions, while an attachment-less row should show upload.
