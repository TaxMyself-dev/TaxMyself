# KT-025 - Release business editing and section-based account codes

- Status: `WORKER_COMPLETE`
- Task manager: Primary integration manager
- Worker: Primary manager (direct execution; no delegated agent requested)
- Worktree: `C:\Users\harel\.codex\worktrees\kt024\taxmyself-dev`
- Branch: `codex/kt-024-release-controls`
- Base commit: `84c85a684c633859bae502085ed949f78aff6d58`

## Business outcome

An accountant can maintain reporting and advance-payment details for an actively
represented client's business from Settings, and an admin creating a SYSTEM
booking account sees and receives the next code in the selected section.

## Acceptance criteria

- [x] An accountant acting for a client through an ACTIVE delegation with the
      required write scope can update that client's existing business details.
- [x] A represented client and unrelated actors retain their current access;
      accountants cannot create or delete the client's businesses through this change.
- [x] The admin card form previews the next SYSTEM code for the selected section.
- [x] Creating a card without a manual code allocates the next free code in that
      section in increments of 10 (for example 60010, 60020 -> 60030).
- [x] The backend remains authoritative and collision-safe when preview state is stale.
- [x] Focused backend tests, Nest build and Angular production build pass;
      frontend tests run when the repository runner can isolate them reliably.

## Scope and constraints

- In scope: business update authorization and Settings UI; SYSTEM admin account
  code preview/allocation by accounting section; tests and topic documentation.
- Out of scope: schema/data migration, production access/deployment, business
  creation/deletion by accountants, accountant/client code-range redesign, tax-law changes.
- Required reading: root/topic AGENTS.md files, coordination workflow, and the full
  categories/accounting redesign master plan.
- Approval boundaries: Elazar explicitly approved these two pre-deployment behavior
  changes on 2026-09-16; no schema or production mutation is authorized.

## Handoff

- Result: Complete. Delegated business update is write-scope gated; SYSTEM card
  codes are previewed per section and recalculated under a section-row lock.
- Commit hash(es):
- Changed files: business controller/spec; bookkeeping allocator, catalog and
  admin controller/specs; Angular catalog service/component/template/spec;
  topic AGENTS docs; this task file.
- Tests and exact results:
  - Backend focused Jest: 4 suites, 61 tests passed.
  - Backend Nest build: passed.
  - Frontend Angular production build on local Node 22: passed with existing
    style/CommonJS/initial-bundle budget warnings.
  - Frontend Angular development build on runtime Node 24: passed.
- Known pre-existing failures: focused Karma `--include` still compiles every
  legacy spec and fails before execution on stale `async` imports, renamed
  components, missing Node typings and other unrelated suite errors. The new
  service spec is present but did not execute in that runner.
- Schema / production / security / accounting / external impact: no schema,
  data migration, production mutation or external-contract change. Security
  expands only delegated PATCH business/update with ACTIVE `DOCUMENTS_WRITE`;
  create/delete remain blocked. Accounting allocation changes only automatic
  SYSTEM admin creation and preserves owner ranges/unique constraints.
- Risks and manual checks: preview may change if another admin creates first;
  POST intentionally recalculates under a pessimistic section lock. Manually
  verify represented-client Settings save and the read-only preview in admin.
- Documentation updated: business, bookkeeping and booking-account-catalog
  topic AGENTS.md files.

## Integration

- Reviewed by:
- Integrated commit:
- Combined verification:
- `origin/main` verification:
- User-visible run instructions:
