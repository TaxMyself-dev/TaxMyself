# KT-026 - Allow active delegated accountants to update business details

- Status: `WORKER_COMPLETE`
- Task manager: Primary integration manager
- Worker: Primary manager
- Worktree: `C:\Users\harel\.codex\worktrees\kt026\taxmyself-dev`
- Branch: `codex/kt-026-business-delegation-scope`
- Base commit: `74ea6f54`

## Outcome and acceptance

- [x] Any ACTIVE accountant-client delegation may update the represented
      client's existing business details, including reporting status and advances.
- [x] Business create/delete remain forbidden to delegated accountants.
- [x] Unrelated actors and inactive delegations remain blocked by the guard.
- [x] Focused tests and Nest build pass.
- [x] No schema, data migration or production mutation is required.

## Cause

KT-025 explicitly required `DOCUMENTS_WRITE`. Production contains valid
view-only delegations created by the owner permission flow with
`DOCUMENTS_READ` plus `EXPENSES_APPROVE`, so the guard rejected the PATCH even
though the accountant-client relationship was ACTIVE. The business-maintenance
capability is tied to the active relationship, not document-write permission.

## Handoff

- Result: `PATCH /business/update` now requires the active-relationship
  `DOCUMENTS_READ` scope instead of document-write permission.
- Commit: recorded by the integration manager after the worker commit.
- Tests: 27/27 focused Jest tests passed; Nest backend build passed.
- Impact and risks: Low, authorization-only change. Create/delete remain
  owner-only; inactive or unrelated actors are still rejected by the guard.
- Documentation: Updated `backend/src/business/AGENTS.md` and this task file.
