# KT-015 - Reports endpoint authorization hardening

- Status: `INTEGRATED`
- Task manager: primary integration manager
- Worker: Codex side conversation
- Worktree: `C:\Users\user\Keepintax\TaxMyself`
- Branch: `auth-fix`
- Base commit: `e8bba5d5ecbd8167667723f68864b385855f998f`

## Business outcome

Financial document summaries are available only for a business owned by the
authenticated effective user, and the uniform-file debug upload is available
only to a platform administrator.

## Acceptance criteria

- [x] `GET /reports/summary` requires Firebase authentication.
- [x] The summary query verifies `(businessNumber, firebaseId)` ownership before reading documents.
- [x] `POST /reports/upload-and-debug` requires Firebase authentication and an ADMIN actor.
- [x] Focused authorization tests and the Nest build pass.

## Scope and constraints

- In scope: the two report endpoints, their shared-service authorization checks, focused tests, and reports topic documentation.
- Out of scope: global-guard policy, upload size/type policy, other report routes, schema, production data, and deployment.
- Required reading: root instructions, reports/guards/users topic instructions, coordination workflow/authority/quality gates, redesign audit and master plan.
- Approval boundaries: exactly the two authorization fixes approved by Elazar; no broader security-policy changes.

## Handoff

- Result: complete
- Commit hash(es): `b67156d7`
- Changed files: `.gitignore`; guards Admin implementation/topic docs;
  reports controller/service/module/security spec/topic docs; this task file.
- Tests and exact results:
  - `npx jest src/reports/reports.endpoint-security.spec.ts src/reports/reports.review-scope.spec.ts src/reports/reports.pnl-pdf.spec.ts --runInBand --forceExit` — 3 suites, 33 tests passed.
  - `npm run build` — Nest build passed with exit code 0.
- Known pre-existing failures: `reports.service.spec.ts` cannot construct
  `ReportsService` because its legacy TestingModule provides none of the
  service's existing dependencies; the changed code adds no constructor
  dependency. In the same run the other 2 suites passed (25 tests).
- Schema / production / security / accounting / external impact: no schema,
  data, accounting-total, or external-contract change. Security is tightened
  on the two approved endpoints. No deployment was performed.
- Risks and manual checks: upload size/type limits remain outside this task;
  the parser is now reachable only after Firebase and ADMIN-actor guards pass.
- Documentation updated: `backend/src/guards/AGENTS.md` and
  `backend/src/reports/AGENTS.md`.

## Integration

- Reviewed by: primary integration manager
- Integrated commit: integration merge commit pending final remote delivery
- Combined verification: 12 focused Jest suites / 123 tests passed; Nest build passed.
- `origin/main` verification: pending
- User-visible run instructions: no manual migration; deploy backend code normally.
